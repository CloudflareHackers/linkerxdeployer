#!/bin/bash

set -e

# Install dependencies
apt-get update
apt-get install -y \
    build-essential \
    ca-certificates \
    curl \
    git \
    libcurl4-gnutls-dev \
    libcurl4-openssl-dev \
    zlib1g-dev

# Confirm installation
git --version